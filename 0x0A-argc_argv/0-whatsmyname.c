#include <stdio.h>
/**
 * main - check the code
 * @argc: number of arguments
 * @argv: vector which have arguments
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - 1]);
	return (0);
}
