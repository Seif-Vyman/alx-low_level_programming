#include "main.h"
#include <stdio.h>
#include <string.h>

/**
* _strlen - converts number of n to 98 by pointers
* @s: pointer of string
*
* Return: strlen is function to get  lenght of string
*/
int _strlen(char *s)
{
	return (strlen(s));
}
